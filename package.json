{
  "name": "flat-data-model-login",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "user": "node scripts/generate-user.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
